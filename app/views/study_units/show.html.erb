<%- model_class = StudyUnit -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:description) %>:</strong></dt>
  <dd><%= @study_unit.description %></dd>
  <dt><strong><%= model_class.human_attribute_name(:course_module_id) %>:</strong></dt>
  <dd><%= @study_unit.course_module_id %></dd>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              study_units_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_study_unit_path(@study_unit), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              study_unit_path(@study_unit),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Você tem certeza?')) },
              :class => 'btn btn-danger' %>
