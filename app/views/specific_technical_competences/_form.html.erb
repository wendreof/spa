<%= form_for @specific_technical_competence, :html => { :class => "form-horizontal specific_technical_competence" } do |f| %>

  <% if @specific_technical_competence.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@specific_technical_competence.errors.count, "error") %> prohibited this specific_technical_competence from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @specific_technical_competence.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :description, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :description, :class => 'form-control' %>
    </div>
    <%=f.error_span(:description) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit "Salvar", :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                specific_technical_competences_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
